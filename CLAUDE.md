# Notes for Claude Code

## Keep the README current

Whenever you open a PR that adds, removes, or renames a public surface
(service, exported method, option, type), update `README.md` in the same
PR so the **Status** table reflects what the package actually exposes.
A reviewer should be able to land the PR and trust the README.
